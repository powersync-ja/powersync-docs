```swift
func watchPendingLists(_ callback: @escaping (_ lists: [ListContent]) -> Void ) async {
    do {
        for try await result in try db.watch(
            sql: "SELECT * FROM lists WHERE state = ?",
            parameters: ["pending"],
        ) { cursor in
            try ListContent(
                id: cursor.getString(name: "id"),
                name: cursor.getString(name: "name")
            )
        } {
            callback(result)
        }
    } catch {
        print("Error in watch: \(error)")
    }
} 